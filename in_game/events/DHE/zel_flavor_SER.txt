namespace = zel_flavor_ser
zel_flavor_ser.1 = { #Novo Brdo grows
	hide_portraits = yes
	type = country_event
	title = zel_flavor_ser.1.title
	desc = zel_flavor_ser.1.desc
	outcome = neutral

	fire_only_once = yes
	dynamic_historical_event = {
		tag = SER
		from = 1387.1.1
		monthly_chance = 100
	}

	trigger = {
		owns = location:novo_brdo
		location:novo_brdo = {
			population >= 10
			OR = {
				location_rank = location_rank:town
				location_rank = location_rank:city
			}
		}
	}

	immediate = {
		location:novo_brdo = {
			add_location_modifier = {
				modifier = zel_ser_nov_mine
				years = -1
				mode = add
			}
			change_max_raw_material_workers = 4
		}
	}

	option = {
		name = zel_flavor_ser.1.a
		historical_option = yes
		show_as_tooltip = {
			location:novo_brdo = {
				add_location_modifier = {
					modifier = zel_ser_nov_mine
					years = -1
					mode = add
				}
				change_max_raw_material_workers = 4
			}
		}
	}

}